extends layout

block content
  h1 #{title}
  form(method='POST', action='/utilisateurs/add')
    #form-group
      label UserName:
      input.form-control(name='username', type='text')
    br
    input.btn.btn-primary(type='submit',value='Submit')
